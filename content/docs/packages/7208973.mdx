---
title: "X-Ample Queue [UN-ESCROWED]"
summary: "Install, configure, and troubleshoot the un-escrowed X-Ample Queue package."
updated: "2026-01-19"
---

## Overview

Un-escrowed variant of X-Ample Queue.

Store page: `/store/package/7208973`

## Install

Follow the same steps as the standard package unless the download includes different folder naming.

If the resource is named `x-ample_queue`:

```
ensure x-ample_queue
```

## Configuration

See `/docs/package/7208967` for the full configuration breakdown (Discord + queue UI settings).

